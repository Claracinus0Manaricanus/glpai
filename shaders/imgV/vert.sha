#version 450 core

layout(location=0) in vec3 pos;
layout(location=3) in vec2 tex;
out vec2 texC;

layout(std430, binding=4) buffer MD{
	mat4 OVM;
};

void main(){
	texC = tex;
	gl_Position = transpose(OVM) * vec4(pos.xy,0,1);
}